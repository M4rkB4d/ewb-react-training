# EWB Banking Portal — Level 2: First App

You're on the **Level 2 Start** branch.

## What's Here

- Everything from Level 1 (types, utilities) — completed
- `portal/` — Full Vite project with Tailwind CSS configured
- `portal/src/components/` — Empty directory for your first components

## What You'll Build

In Level 2, you'll set up the project and create your first components and tests:

1. Create `Greeting.tsx` and `Counter.tsx` components
2. Build a simple dashboard page
3. Write your first test with Vitest + Testing Library
4. Learn Tailwind CSS 4 with the EWB design system theme

## Getting Started

```bash
cd portal
npm install
npm run dev
```

## When You're Done

```bash
git diff level-02-start level-02-complete
git checkout level-03-start && npm install
```
