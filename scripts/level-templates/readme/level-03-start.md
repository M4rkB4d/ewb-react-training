# EWB Banking Portal — Level 3: Building UI

You're on the **Level 3 Start** branch.

## What's Here

- Everything from Levels 1-2 — completed
- Basic components (`Greeting`, `Counter`) working
- `portal/src/components/ui/` — Empty directory for your design system

## What You'll Build

In Level 3, you'll create the EWB design system and build production-quality forms:

1. Design system components: Button, Input, Card, Badge, Alert
2. Forms with Zod validation and React Hook Form
3. Accessibility essentials (skip links, ARIA, focus management)
4. A design system showcase page

## New Dependencies

This level introduces: `zod`, `react-hook-form`, `@hookform/resolvers`

## Getting Started

```bash
cd portal
npm install
npm run dev
```

## When You're Done

```bash
git diff level-03-start level-03-complete
git checkout level-04-start && npm install
```
