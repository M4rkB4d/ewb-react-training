# EWB Banking Portal — Level 8: Mastery

You're on the **Level 8 Start** branch.

## What's Here

- Everything from Levels 1-7 — completed
- Production-ready portal with security, compliance, CI/CD
- `portal/src/i18n/` — Empty directory for internationalization
- `portal/src/features/transfers/` — Empty feature module
- `portal/src/features/payments/` — Empty feature module

## What You'll Build

Level 8 is the capstone. You'll add enterprise-grade features:

1. Internationalization (English, Filipino, Chinese) with react-intl
2. Real-time patterns (WebSocket, SSE, polling)
3. Transfer wizard with multi-step form
4. Payment system with biller search
5. Integration capstone — tying everything together

## New Dependencies

This level introduces: `react-intl`, `@formatjs/intl-localematcher`, `date-fns`, `lucide-react`, `uuid`, `react-window`, `react-virtualized-auto-sizer`

## Getting Started

```bash
cd portal
npm install
npm run dev
```

## When You're Done

```bash
git diff level-08-start level-08-complete
git checkout level-09-start && npm install
```
